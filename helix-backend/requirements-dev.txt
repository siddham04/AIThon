# Helix backend — dev / test dependencies.
#
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Then run the golden-domain pipeline contract:
#   pytest tests/test_golden_pipeline.py -v
#
# See docs/GOLDEN_DOMAIN.md for the bulletproof contract.

-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
